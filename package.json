{
  "name": "my-terra-dapp",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "deploy:marketplace_local": "terrain deploy marketplace --signer=validator --set-signer-as-admin",
    "deploy:gen0_local": "terrain deploy gen0 --signer=validator --set-signer-as-admin",
    "deploy:marketplace_test": "terrain deploy marketplace --signer=bombay --set-signer-as-admin --network testnet",
    "deploy:gen0_test": "terrain deploy gen0 --signer=bombay --set-signer-as-admin --network testnet",
    "sync-refs": "terrain sync-refs --dest=./frontend/src/refs.terrain.json"
  },
  "dependencies": {
    "@iboss/terrain": "0.0.8"
  },
  "workspaces": [
    "frontend"
  ],
  "author": "",
  "license": "ISC"
}
