apiVersion: v1
kind: Service
metadata:
  name: {{ include "no-fucking-thing-backend.fullname" . }}
  labels:
    {{- include "no-fucking-thing-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "no-fucking-thing-backend.selectorLabels" . | nindent 4 }}
